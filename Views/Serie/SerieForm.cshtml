@model BootFlixBC9.ViewModels.SerieFormViewModel
@{
    ViewBag.Title = "SerieForm";
}
<h2>@Model.Title</h2>
@using (Html.BeginForm("Save", "Serie"))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "Please fix the following errors:")
<div class="form-group">
    @Html.LabelFor(s => s.Name)
    @Html.TextBoxFor(s => s.Name, new { @class = "form-control" })
    @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
</div>
<div class="form-group">
    @Html.LabelFor(s => s.DateReleased)
    @Html.TextBoxFor(s => s.DateReleased, "{0:d MMM yyyy}", new { @class = "form-control" })
    @Html.ValidationMessageFor(model => model.DateReleased, "", new { @class = "text-danger" })
</div>
    <div class="form-group">
        @Html.LabelFor(s => s.GenreId)
        @Html.DropDownListFor(s => s.GenreId, new SelectList(Model.Genres, "Id", "Name"), "=Select=", new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(s => s.Seasons)
        @Html.TextBoxFor(s => s.Seasons, new { @class = "form-control" })
        @Html.ValidationMessageFor(model => model.DateReleased, "", new { @class = "text-danger" })
    </div>
    @Html.HiddenFor(s => s.Id)
    <button type="submit" class="btn btn-primary">Save</button>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval");
}